defaults : &defaults
  adapter: postgresql
  encoding: unicode
  pool: 5
  username:
  password:

development: &DEV
  <<: *defaults
  database: train-rec_development

test: &TEST
  <<: *defaults
  database: train-rec_test

production:
  <<: *defaults
  database: train-rec_production
