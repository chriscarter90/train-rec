defaults : &defaults
  adapter: mysql2
  username: root
  password:
  pool: 5
  timeout: 5000

development: &DEV
  <<: *defaults
  database: progress360_development

test: &TEST
  <<: *defaults
  database: progress360_test

production:
  <<: *defaults
  database: progress360_production
