<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, minimal-ui, user-scalable=no" />
  <title><%= "%s - " % yield(:page_title) if content_for?(:page_title) %>Train Rec</title>
  <%= stylesheet_link_tag "bootstrap.min.css", media: "all" %>
  <%= stylesheet_link_tag "flat-ui-pro", media: "all" %>
  <%= stylesheet_link_tag "unicons", media: "all" %>
  <%= stylesheet_link_tag "responsive", media: "all" %>
  <%= csrf_meta_tags %>
</head>
<body>
  <nav class="navbar navbar-default" role="navigation">
    <div class="container">
      <div class="row">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#navbar-collapse-01">
            <span class="sr-only">Toggle navigation</span>
          </button>
          <a class="navbar-brand" href="<%= root_path %>"><span class="icon-rec"></span>&nbsp;Train Rec</a>
        </div>
        <div class="collapse navbar-collapse" id="navbar-collapse-01">
          <% if current_user %>
          <ul class="nav navbar-nav navbar-right">
            <li><a href="<%= logout_path %>">Log out</a></li>
          </ul>
        <% end %>
        </div><!-- /.navbar-collapse -->
      </div>
    </div>
  </nav><!-- /navbar -->

  <div class="container">
    <%= yield %>
  </div>

  <%= javascript_include_tag "application" %>
</body>
</html>
