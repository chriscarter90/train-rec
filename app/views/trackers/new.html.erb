<% content_for :page_title, "New Tracker" %>

<div class="l-standalone-page">
  <h3 class="heading pane-header trackers-pane-header">Create new tracker</h3>

  <%= form_for resource do |f| %>
    <%= render 'form', :f => f %>
  <% end %>
</div>
